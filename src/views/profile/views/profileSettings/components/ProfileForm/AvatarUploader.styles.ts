import styled from 'styled-components'
import { Button as BaseButton, ImageUploader as BaseImageUploader } from '../../../../../../components'

const Button = styled(BaseButton)``
const ImageUploader = styled(BaseImageUploader)``

export { Button, ImageUploader }
