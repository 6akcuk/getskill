import styled from 'styled-components'
import { Tabs } from '../../../../components'

const Wrapper = styled.div``
const FeedTabs = styled(Tabs)``
const Content = styled.div`
  margin-top: 1.5rem;
`

export { Content, FeedTabs, Wrapper }
