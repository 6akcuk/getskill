import styled from 'styled-components'
import { EntityListWrapper } from '../../../../components'

const Wrapper = styled.div``
const ListWrapper = styled(EntityListWrapper)``

export { ListWrapper, Wrapper }
